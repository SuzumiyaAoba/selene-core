/// エントリーポイント。REPL を起動する。
///
/// JS バックエンドでインタラクティブ REPL を使用する場合は、
/// run() を run_interactive() に変更してください。
fn main {
  // run()  // サンプル入力を使用するバージョン
  run_interactive()  // 標準入力から読み取るバージョン（JS バックエンド専用）
}
