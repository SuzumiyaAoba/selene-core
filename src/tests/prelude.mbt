/// Package-level imports for tests.
pub using @core {
  type Value,
  type Number,
  type SExpr,
  type Env,
  type EvalError,
  type Procedure,
  type Promise,
  type Port,
  type PortDirection,
  type Continuation,
  type ContinuationCall,
  type SyntaxRules,
  type SyntaxRule,
  type VMCallFrame,
  empty_env,
  extend,
  lookup,
  define_var,
  set_bang,
  list2,
  sym,
  make_pair,
}
pub using @frontend { type Token, tokenize, parse, parse_one, parse_tokens, expand_quote }
pub using @runtime {
  eval,
  initial_env,
  create_initial_globals,
  new_vm_state,
  new_vm_state_with_globals,
  vm_run,
  vm_eval,
  type VMError,
  get_gc_stats,
  reset_gc_stats,
  gc_stats_to_string,
  gc_mark_continuation,
  gc_register_continuation,
  gc_sweep_continuations,
  gc_clear_vm,
  gc_clear_all_continuations,
  gc_track_value,
  gc_track_closure,
  gc_track_pair,
  gc_track_vector,
  gc_track_box,
  get_gc_config,
  set_gc_config,
}
pub using @compiler {
  type IRExpr,
  type IRConst,
  type CodeChunk,
  type CompiledModule,
  type Opcode,
  type DumpOptions,
  type PrimOpKind,
  compile_toplevel,
  compile_toplevel_with_globals,
  optimize,
  generate,
  compile_to_module,
  compile_to_module_optimized,
  initial_globals,
  default_dump_options,
  compact_dump_options,
  dump_module,
  dump_module_stats,
  dump_chunk,
  compile_and_dump_default,
}
pub using @repl { type VMReplContext, new_repl_context, repl_eval, repl_eval_expr }
pub using @debug {
  type DebugEvent,
  type SerializeError,
  new_debugger,
  debug_run,
  add_breakpoint,
  add_breakpoint_main,
  remove_breakpoint,
  enable_breakpoint,
  disable_breakpoint,
  clear_breakpoints,
  list_breakpoints,
  debug_step,
  debug_stack_to_string,
  debug_call_stack_to_string,
  debug_get_location,
  debug_get_current_instruction,
  debug_disassemble,
  debug_locals_to_string,
  debug_globals_to_string,
  debug_history_to_string,
  debug_summary,
  new_profiler,
  profiler_reset,
  profiler_enable,
  profiler_disable,
  profiler_record_instruction,
  profiler_record_call,
  profiler_record_return,
  profiler_instruction_report,
  profiler_function_report,
  profiler_hotspot_report,
  profiler_summary,
  profiler_full_report,
  vm_eval_with_profiler,
  serialize_module_to_string,
  deserialize_module_from_string,
  serialize_module_to_binary,
  deserialize_module_from_binary,
}
